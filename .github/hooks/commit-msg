#!/usr/bin/env bash
# Reject commit messages that do not follow docs/COMMIT_RULES.md, before the
# commit is even created. Install with: .github/scripts/install-hooks.sh
set -euo pipefail
repo_root=$(git rev-parse --show-toplevel)
exec "${repo_root}/.github/scripts/validate-commit-msg.sh" --file "$1"
